import React from "react";
import PortfolioForm from "../components/PortfolioForm";

const PortfolioPage = ({ onComplete }) => {
  return <PortfolioForm onComplete={onComplete} />;
};

export default PortfolioPage;